# روتین پلنر — نسخه‌ی اصلاح‌شده + بسته‌بندی Capacitor برای اندروید

## چی درست شد (در `www/index.html`)

1. **رنگ پس‌زمینه تاریک/روشن نمی‌شد:** صفحه هیچ‌جا به مرورگر اعلام نمی‌کرد که خودش هم حالت روشن و هم تاریک را پشتیبانی می‌کند (`color-scheme`). در نتیجه سیستم «تیره‌سازی خودکار» اندروید/کروم رنگ‌های خودِ برنامه را نادیده می‌گرفت و همه‌چیز را خودش تیره می‌کرد. حالا این را هم در `<meta>` و هم در CSS و هم در جاوااسکریپت (هماهنگ با تم فعلی) اعلام کردیم؛ اسلایدر تنظیمات از این به بعد واقعاً رنگ را عوض می‌کند.
2. **نمی‌شد در نمودار علت‌ها دلیل ثبت کرد:** این بخش قبلاً فقط *نمایش‌دهنده*‌ی دلایلی بود که هنگام ثبت موفقیت/شکست یک عادت وارد می‌کردی، و هیچ فرم افزودنی نداشت. یک فرم کوچک «➕ ثبت دلیل» (انتخاب عادت + موفقیت/شکست + متن) به همان تب اضافه شد تا مستقیماً هم بشود دلیل ثبت کرد.
3. **آیکون رعد و برق (⚡) کاری نمی‌کرد:** این دکمه به یک پنل با `display:none` وصل بود که هیچ کد دیگری در کل برنامه آن را نمایان نمی‌کرد — یعنی از اول کار نمی‌کرد. حالا مستقیم مودال نمودارهای همان عادت (که در برنامه فعال و تست‌شده است) را باز می‌کند، دقیقاً روی تب «محرک».
4. **نوتیفیکیشن لیست‌کارها/یادآوری عادت‌ها اصلاً نمی‌آمد:** اینجا سه مشکل هم‌زمان بود:
   - فایل `sw.js` که در `index.html` رجیستر می‌شد اصلاً در آپلود شما وجود نداشت؛ بدون آن، مسیر اصلی نمایش نوتیفیکیشن (`navigator.serviceWorker.ready`) هیچ‌وقت اجرا نمی‌شد.
   - همه‌چیز پشت یک تیک عمومی «هشدار درون‌برنامه‌ای» بود که هم گیج‌کننده بود و هم فقط وقتی برنامه باز بود کار می‌کرد.
   - مهم‌تر از همه: نوتیفیکیشن *واقعی سرِ ساعت حتی وقتی اپ کاملاً بسته است*، با تکنولوژی وب خالص (PWA) قابل‌تضمین نیست — این یک محدودیت پلتفرمی اندروید/کروم است، نه محدودیت این برنامه؛ APIهای وب برای این کار (Periodic Background Sync) به‌صورت غیرقابل‌اعتماد و با فاصله‌ی چند ساعته کار می‌کنند، نه سرِ دقیقه‌ی مشخص.

   **به همین دلیل طبق درخواست شما، مسیر Capacitor را پیاده کردیم:** حالا وقتی همین کد داخل یک اپ اندرویدی واقعی (Capacitor) اجرا شود، از پلاگین رسمی `@capacitor/local-notifications` استفاده می‌کند که با **AlarmManager خودِ اندروید** کار می‌کند — یعنی نوتیفیکیشن سرِ همان لحظه‌ی دقیق می‌آید، حتی اگر اپ کاملاً بسته/kill شده باشد. تیک عمومی حذف شد؛ حالا به‌محض اینکه برای یک کار زمان تعیین کنی یا یادآوری یک عادت را روشن کنی، اول اجازه‌ی نوتیفیکیشن گرفته می‌شود و بعد سرِ همان ساعت هشدار بومی می‌آید. اگر همین فایل را به‌جای اپ، در مرورگر/PWA معمولی باز کنی، به‌طور خودکار به همان روش قبلیِ وب (Service Worker) برمی‌گردد؛ یعنی همین یک `index.html` هم برای اپ اندرویدی و هم برای نسخه‌ی وب/GitHub Pages کار می‌کند.

## ساختار این پوشه

```
RoutinePlannerApp/
├── package.json          ← وابستگی‌های Capacitor
├── capacitor.config.json ← تنظیمات اپ (appId, appName, ...)
└── www/
    ├── index.html         ← همان برنامه‌ی شما، با ۴ اصلاح بالا
    ├── manifest.json      ← یک نمونه‌ی حداقلی (اگر manifest واقعی خودتان را دارید جایگزین کنید)
    ├── sw.js              ← Service Worker برای نسخه‌ی وب/PWA
    └── icons/             ← آیکون‌های تازه‌طراحی‌شده (۴۸، ۱۹۲، ۵۱۲ و اپل-تاچ)
```

✅ یک ست آیکون (`icons/icon-192.png`, `icon-512.png`, `apple-touch-icon.png`, `favicon-48.png`) هم طراحی و داخل `www/icons/` گذاشته شده — همان تیک/رینگ طلایی که در تایل‌های خودِ برنامه هست. اگر لوگوی دیگری از قبل دارید، کافی‌ست همین فایل‌ها را با همین نام‌ها جایگزین کنید.

## مراحل باقی‌مانده (باید روی سیستم خودتان انجام شود)

من در این چت به اینترنت و Android SDK دسترسی ندارم، پس نمی‌توانم خودم APK بسازم؛ کل کدنویسی انجام شده، فقط این چند دستور را روی کامپیوتر خودتان (با Node.js و Android Studio نصب‌شده) اجرا کنید:

1. (اختیاری) اگر لوگوی دیگری می‌خواهید، آیکون‌های دلخواه‌تان را جایگزین فایل‌های داخل `www/icons/` کنید.
2. در پوشه‌ی `RoutinePlannerApp`:
   ```bash
   npm install
   npx cap add android
   npx cap sync android
   npx cap open android
   ```
3. اندروید استودیو باز می‌شود؛ از همان‌جا با دکمه‌ی Run روی گوشی/شبیه‌ساز نصب کنید، یا برای گرفتن فایل APK: `Build → Build Bundle(s) / APK(s) → Build APK(s)`.
4. برای انتشار در گیت‌هاب (نسخه‌ی PWA/وب موازی)، فایل‌های داخل `www/` (`index.html`, `manifest.json`, `sw.js`, `icons/`) را همان‌طور که هستند در ریپو بگذارید — چیزی عوض نمی‌خواهد.

### نکته‌ی مجوزها
پلاگین `@capacitor/local-notifications` خودش مجوزهای لازم (`POST_NOTIFICATIONS` برای اندروید ۱۳+ و در صورت نیاز `SCHEDULE_EXACT_ALARM`) را به‌طور خودکار به `AndroidManifest.xml` اضافه می‌کند؛ کد داخل برنامه هم همان لحظه‌ای که تاریخ/ساعت یادآوری را تنظیم می‌کنی، درخواست اجازه را نشان می‌دهد — نیازی به کار دستی نیست.
